// import { io } from 'socket.io-client';

// export let socket = io.connect("http://localhost:3001"); 
// export let socketID = socket.on('connect', () => {
//     socketID = socket.id
//     console.log(socket.id);
// });